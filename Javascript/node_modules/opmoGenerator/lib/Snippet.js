var Opmo = require('./Opmo');

class Snippet extends Opmo {
  constructor(data){
    super(data);
  }
  generate(){
    var opmo = super.genBody() + super.genFooter();
    super.exportFile(opmo);
  }
}

module.exports = Snippet;
