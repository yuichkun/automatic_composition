module.exports = {
  renda : {
    //renda-pitch-num - & rest
    pitch : {
      func : 'gen-repeat',
      args: ['this.args[1]', 'this.args[0]']
    },
    length : {
      func: `stretch`,
      args: ['this.timeSig', 'pitch', 'this.args[2]']
    }
  },
  ascend : {
    pitch : {
      func: 'ascend',
      args: ['this.args[0]', 'this.args[1]', 'this.args[2]']
    },
    length : {
      func: `stretch`,
      args: ['this.timeSig', 'pitch']
    }
  },
  random : {
    pitch: {
      // return : '(tonality-map \'(minor-pentatonic) (integer-to-pitch (rnd-row)))'
      func: 'integer-to-pitch',
      args: ['(rnd-row)']
    },
    length: {
      func: `length-weight`,
      args: ['(span pitch \'(s))']
    }
  }
};
